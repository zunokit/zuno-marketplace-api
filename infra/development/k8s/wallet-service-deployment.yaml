apiVersion: v1
kind: Service
metadata:
  name: wallet-service
  namespace: dev
spec:
  ports:
    - port: 50053
      targetPort: 50053
  selector:
    app: wallet-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-service
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wallet-service
  template:
    metadata:
      labels:
        app: wallet-service
    spec:
      containers:
        - name: wallet-service
          image: wallet-service
          imagePullPolicy: Never
          ports:
            - containerPort: 50053
          env:
            - name: WALLET_GRPC_PORT
              value: ":50053"
            - name: POSTGRES_HOST
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: POSTGRES_HOST
            - name: POSTGRES_PORT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: POSTGRES_PORT
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: POSTGRES_DATABASE
